import { createTheme } from "@mui/material/styles";
export const colorRed="red"
const theme = createTheme({
  typography: {
    fontFamily: ["Vazir", "Vazir-Bold", "Vazir-Medium"].join(",")
  }
});

export default theme;
